# 🚀 StandPro Backend (NestJS)

Este é o serviço de backend da plataforma StandPro.

## 🛠️ Tecnologias
- **Framework:** NestJS (Node.js)
- **ORM:** Prisma
- **Base de Dados:** PostgreSQL
- **Cache/Session:** Redis
- **Documentação:** Swagger (OpenAPI)

## 🚀 Como Executar
1. Instale as dependências: `npm install`
2. Configure o `.env`
3. Execute as migrations: `npx prisma migrate deploy`
4. Inicie o servidor: `npm run start:dev`

## 📚 API
A documentação da API está disponível em `/api/docs` após o início do servidor.
